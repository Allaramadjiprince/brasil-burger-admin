{% extends 'base.html.twig' %}

{% block title %}Nouveau Livreur - Brasil Burger Admin{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg">
                <div class="card-header bg-red text-white">
                    <div class="d-flex justify-content-between align-items-center">
                        <h4 class="mb-0">
                            <i class="fas fa-user-plus me-2"></i>Ajouter un Nouveau Livreur
                        </h4>
                        <a href="{{ path('admin_livreur_index') }}" class="btn btn-sm btn-light">
                            <i class="fas fa-arrow-left me-1"></i>Retour
                        </a>
                    </div>
                </div>
                
                <div class="card-body">
                    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            {{ form_label(form.nom, 'Nom', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.nom, {'attr': {'class': 'form-control' ~ (form.nom.vars.valid ? '' : ' is-invalid'), 'placeholder': 'Ex: DIALLO'}}) }}
                            <div class="invalid-feedback">
                                {{ form_errors(form.nom) }}
                            </div>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            {{ form_label(form.prenom, 'Prénom', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.prenom, {'attr': {'class': 'form-control' ~ (form.prenom.vars.valid ? '' : ' is-invalid'), 'placeholder': 'Ex: Mamadou'}}) }}
                            <div class="invalid-feedback">
                                {{ form_errors(form.prenom) }}
                            </div>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            {{ form_label(form.telephone, 'Téléphone', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            <div class="input-group">
                                <span class="input-group-text">+221</span>
                                {{ form_widget(form.telephone, {'attr': {'class': 'form-control' ~ (form.telephone.vars.valid ? '' : ' is-invalid'), 'placeholder': '77 123 45 67'}}) }}
                            </div>
                            <div class="invalid-feedback">
                                {{ form_errors(form.telephone) }}
                            </div>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            {{ form_label(form.vehicule, 'Véhicule', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.vehicule, {'attr': {'class': 'form-select' ~ (form.vehicule.vars.valid ? '' : ' is-invalid')}}) }}
                            <div class="form-text">Sélectionnez le type de véhicule utilisé</div>
                        </div>
                    </div>
                    
                    <div class="mb-3">
                        <div class="form-check form-switch">
                            {{ form_widget(form.disponible, {'attr': {'class': 'form-check-input'}}) }}
                            {{ form_label(form.disponible, 'Livreur disponible immédiatement', {'label_attr': {'class': 'form-check-label fw-bold'}}) }}
                        </div>
                        <div class="form-text">
                            Si désactivé, le livreur n'apparaîtra pas pour les nouvelles livraisons
                        </div>
                    </div>
                    
                    <div class="mt-4 border-top pt-3">
                        <div class="d-flex justify-content-between">
                            <a href="{{ path('admin_livreur_index') }}" class="btn btn-secondary">
                                <i class="fas fa-times me-1"></i>Annuler
                            </a>
                            <button type="submit" class="btn btn-red btn-lg">
                                <i class="fas fa-save me-1"></i>Créer le Livreur
                            </button>
                        </div>
                    </div>
                    
                    {{ form_end(form) }}
                </div>
            </div>
            
            <div class="card mt-3">
                <div class="card-body">
                    <h6 class="card-title"><i class="fas fa-info-circle me-2"></i>Informations importantes</h6>
                    <ul class="list-unstyled">
                        <li><i class="fas fa-check text-success me-2"></i>Un livreur peut gérer jusqu'à 3 commandes simultanément</li>
                        <li><i class="fas fa-check text-success me-2"></i>Les livreurs disponibles apparaissent dans l'application mobile</li>
                        <li><i class="fas fa-check text-success me-2"></i>Le numéro de téléphone doit être valide pour les notifications</li>
                        <li><i class="fas fa-check text-success me-2"></i>Vous pouvez modifier la disponibilité à tout moment</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
// Validation Bootstrap
(function () {
    'use strict'
    var forms = document.querySelectorAll('.needs-validation')
    Array.prototype.slice.call(forms).forEach(function (form) {
        form.addEventListener('submit', function (event) {
            if (!form.checkValidity()) {
                event.preventDefault()
                event.stopPropagation()
            }
            form.classList.add('was-validated')
        }, false)
    })
})()
</script>
{% endblock %}