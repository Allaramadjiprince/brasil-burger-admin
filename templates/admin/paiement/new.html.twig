{% extends 'base.html.twig' %}

{% block title %}Nouveau Paiement - Brasil Burger Admin{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg">
                <div class="card-header bg-red text-white">
                    <div class="d-flex justify-content-between align-items-center">
                        <h4 class="mb-0">
                            <i class="fas fa-plus-circle me-2"></i>Enregistrer un Paiement
                        </h4>
                        <a href="{{ path('admin_paiement_index') }}" class="btn btn-sm btn-light">
                            <i class="fas fa-arrow-left me-1"></i>Retour
                        </a>
                    </div>
                </div>
                
                <div class="card-body">
                    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            {{ form_label(form.commande, 'Commande', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.commande, {'attr': {'class': 'form-select' ~ (form.commande.vars.valid ? '' : ' is-invalid')}}) }}
                            <div class="form-text">Sélectionnez la commande associée</div>
                            <div class="invalid-feedback">
                                {{ form_errors(form.commande) }}
                            </div>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            {{ form_label(form.montant, 'Montant (XOF)', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            <div class="input-group">
                                {{ form_widget(form.montant, {'attr': {'class': 'form-control' ~ (form.montant.vars.valid ? '' : ' is-invalid'), 'placeholder': '0.00'}}) }}
                                <span class="input-group-text">XOF</span>
                            </div>
                            <div class="invalid-feedback">
                                {{ form_errors(form.montant) }}
                            </div>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            {{ form_label(form.methode, 'Méthode de paiement', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.methode, {'attr': {'class': 'form-select' ~ (form.methode.vars.valid ? '' : ' is-invalid')}}) }}
                            <div class="invalid-feedback">
                                {{ form_errors(form.methode) }}
                            </div>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            {{ form_label(form.statut, 'Statut', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.statut, {'attr': {'class': 'form-select' ~ (form.statut.vars.valid ? '' : ' is-invalid')}}) }}
                        </div>
                    </div>
                    
                    <div class="mb-3">
                        {{ form_label(form.date, 'Date du paiement', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.date, {'attr': {'class': 'form-control' ~ (form.date.vars.valid ? '' : ' is-invalid')}}) }}
                    </div>
                    
                    <div class="mt-4 border-top pt-3">
                        <div class="d-flex justify-content-between">
                            <a href="{{ path('admin_paiement_index') }}" class="btn btn-secondary">
                                <i class="fas fa-times me-1"></i>Annuler
                            </a>
                            <button type="submit" class="btn btn-red btn-lg">
                                <i class="fas fa-save me-1"></i>Enregistrer le Paiement
                            </button>
                        </div>
                    </div>
                    
                    {{ form_end(form) }}
                </div>
            </div>
            
            <div class="card mt-3">
                <div class="card-body">
                    <h6 class="card-title"><i class="fas fa-info-circle me-2"></i>Informations importantes</h6>
                    <ul class="list-unstyled">
                        <li><i class="fas fa-check text-success me-2"></i>Les paiements sont automatiquement liés aux commandes</li>
                        <li><i class="fas fa-check text-success me-2"></i>Seuls les paiements validés permettent la livraison</li>
                        <li><i class="fas fa-check text-success me-2"></i>Les statuts sont mis à jour automatiquement via les APIs Wave/OM</li>
                        <li><i class="fas fa-check text-success me-2"></i>Un reçu peut être généré après validation</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
// Validation Bootstrap
(function () {
    'use strict'
    var forms = document.querySelectorAll('.needs-validation')
    Array.prototype.slice.call(forms).forEach(function (form) {
        form.addEventListener('submit', function (event) {
            if (!form.checkValidity()) {
                event.preventDefault()
                event.stopPropagation()
            }
            form.classList.add('was-validated')
        }, false)
    })
})()
</script>
{% endblock %}