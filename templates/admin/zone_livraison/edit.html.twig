{% extends 'base.html.twig' %}

{% block title %}Modifier Zone - Brasil Burger Admin{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg">
                <div class="card-header bg-yellow text-dark">
                    <div class="d-flex justify-content-between align-items-center">
                        <h4 class="mb-0">
                            <i class="fas fa-edit me-2"></i>Modifier "{{ zone_livraison.nom }}"
                        </h4>
                        <a href="{{ path('admin_zone_livraison_index') }}" class="btn btn-sm btn-light">
                            <i class="fas fa-arrow-left me-1"></i>Retour
                        </a>
                    </div>
                </div>
                
                <div class="card-body">
                    {{ form_start(form) }}
                    
                    <div class="mb-3">
                        {{ form_label(form.nom, 'Nom de la zone') }}
                        {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    
                    <div class="mb-3">
                        {{ form_label(form.quartiers, 'Quartiers couverts') }}
                        {{ form_widget(form.quartiers, {'attr': {'class': 'form-control', 'rows': 5}}) }}
                    </div>
                    
                    <div class="mb-3">
                        {{ form_label(form.prix, 'Prix de livraison (XOF)') }}
                        <div class="input-group">
                            {{ form_widget(form.prix, {'attr': {'class': 'form-control'}}) }}
                            <span class="input-group-text">XOF</span>
                        </div>
                    </div>
                    
                    <div class="mt-4">
                        <button type="submit" class="btn btn-red btn-lg">
                            <i class="fas fa-save me-1"></i>Enregistrer
                        </button>
                        <a href="{{ path('admin_zone_livraison_index') }}" class="btn btn-secondary ms-2">
                            Annuler
                        </a>
                    </div>
                    
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}