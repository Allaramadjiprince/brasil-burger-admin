<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Brasil Burger Admin{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --brasil-rouge: #FF0000;
            --brasil-jaune: #FFDF00;
            --brasil-bleu: #002776;
        }
        body { background: #f8f9fa; }
        .navbar { background: #FF0000; }
        .sidebar { background: white; min-height: 100vh; }
        .btn-brasil { background: #FF0000; color: white; }
        .card-header { background: rgba(255, 0, 0, 0.1); }
        .nav-link { color: #333; padding: 8px; border-radius: 4px; }
        .nav-link:hover { background: rgba(255, 0, 0, 0.1); text-decoration: none; }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark">
        <div class="container-fluid">
            <span class="navbar-brand">BRASIL BURGER - ADMIN</span>
        </div>
    </nav>
    
    <div class="container-fluid">
        <div class="row">
            <div class="col-2 sidebar p-3">
                <a href="/" class="d-block p-2 mb-2 btn-brasil rounded">Dashboard</a>
                <a href="/admin/commande" class="d-block p-2 mb-1 nav-link">Commandes</a>
                <a href="/admin/client" class="d-block p-2 mb-1 nav-link">Clients</a>
                <a href="/admin/produit" class="d-block p-2 mb-1 nav-link">Produits</a>
                <a href="/admin/livreur" class="d-block p-2 mb-1 nav-link">Livreurs</a>
                <a href="/admin/paiement" class="d-block p-2 mb-1 nav-link">Paiements</a>
                <a href="/admin/zone-livraison" class="d-block p-2 mb-1 nav-link">Zones</a>
            </div>
            
            <div class="col-10 p-4">
                {% block body %}{% endblock %}
            </div>
        </div>
    </div>
</body>
</html>